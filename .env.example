PORT=3000
NODE_ENV=development

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=tiktok_b24

REDIS_HOST=localhost
REDIS_PORT=6379

# TikTok webhook
TIKTOK_WEBHOOK_SECRET=your_test_secret
# set 1 to skip verify in DEV (turn off in production)
SKIP_SIGNATURE_VERIFY=1

# Bitrix REST base (mock/real). Real format:
# https://<portal>.bitrix24.vn/rest/<user_id>/<token>
BITRIX_BASE_URL=https://<portal>.bitrix24.vn/rest/<user>/<token>
BITRIX_TOKEN=dummy    # if mock
BITRIX_DEFAULT_PIPELINE=1
BITRIX_DEFAULT_STAGE=NEW
BITRIX_RATE_MAX=40
BITRIX_RATE_DURATION=1000
